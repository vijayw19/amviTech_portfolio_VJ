{
  "family": "amvitech-portfolio-qa",
  "networkMode": "awsvpc",
  "requiresCompatibilities": ["FARGATE"],
  "cpu": "256",
  "memory": "512",
  "executionRoleArn": "REPLACE_EXECUTION_ROLE_ARN_QA",
  "taskRoleArn": "REPLACE_TASK_ROLE_ARN_QA",
  "containerDefinitions": [
    {
      "name": "web",
      "image": "REPLACE_IMAGE",
      "essential": true,
      "portMappings": [{ "containerPort": 80, "protocol": "tcp" }],
      "environment": [
        { "name": "APP_ENV", "value": "qa" }
      ],
      "logConfiguration": {
        "logDriver": "awslogs",
        "options": {
          "awslogs-region": "REPLACE_AWS_REGION",
          "awslogs-group": "/ecs/amvitech-portfolio-qa",
          "awslogs-stream-prefix": "ecs"
        }
      }
    }
  ]
}
